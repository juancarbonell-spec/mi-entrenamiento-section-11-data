name: Intervals.icu Sync

on:
  schedule:
    - cron: '*/15 * * * *' 
  workflow_dispatch: 

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install requests

      - name: Verificar Credenciales
        run: |
          if [ -z "${{ secrets.INTERVALS_ICU_ID }}" ]; then echo "ERROR: INTERVALS_ICU_ID no encontrado en Secrets."; exit 1; fi
          if [ -z "${{ secrets.INTERVALS_ICU_API_KEY }}" ]; then echo "ERROR: INTERVALS_ICU_API_KEY no encontrado en Secrets."; exit 1; fi
          echo "Credenciales detectadas correctamente."

      - name: Sync Data (Download 30 Activities)
        env:
          ID: ${{ secrets.INTERVALS_ICU_ID }}
          KEY: ${{ secrets.INTERVALS_ICU_API_KEY }}
        run: |
          python -c "
          import requests, os, json
          
          # Limpiamos el ID de la 'i' si existe para que la URL sea válida
          raw_id = os.environ.get('ID', '')
          athlete_id = ''.join(filter(str.isdigit, raw_id))
          api_key = os.environ.get('KEY', '')
          
          url = f'https://intervals.icu/api/v1/athlete/{athlete_id}/activities'
          response = requests.get(url, auth=('API_KEY', api_key), params={'limit': 30})
          
          if response.status_code == 200:
              os.makedirs('data', exist_ok=True)
              with open('data/activities_history.json', 'w') as f:
                  json.dump(response.json(), f, indent=2)
              print('Sincronización completada con éxito.')
          else:
              print(f'Error de API {response.status_code}: {response.text}')
              exit(1)
          "

      - name: Commit and Push
        run: |
          git config --global user.name "Section 11 Bot"
          git config --global user.email "bot@section11.ai"
          git add .
          git commit -m "Sincronización 30 actividades" || exit 0
          git push
